{
  "name": "server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Backend API for the Finance Tracker app",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node --watch --env-file-if-exists=.env index.js",
    "dev:memory": "node --watch scripts/dev-memory.js",
    "start": "node --env-file-if-exists=.env index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "express-rate-limit": "^8.7.0",
    "helmet": "^8.3.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.10.1",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "mongodb-memory-server": "^11.2.0",
    "supertest": "^7.2.2",
    "vitest": "^5.0.1"
  }
}
